[workspace]
channels = ["conda-forge", "bioconda"]
name = "Assembleur"
platforms = ["linux-64"]
version = "0.1.0"

[tasks._preparedir]
cmd = "rm -rf build"

[tasks._configure]
cmd = "cmake -S . -B build"
description = "Configure the C++ project using CMake"
inputs = ["CMakeLists.txt"]
outputs = ["build/Makefile"]

[tasks._configure_static]
cmd = "cmake -S . -B build -DCMAKE_EXE_LINKER_FLAGS='-static'"
description = "Configure the C++ project using CMake"
inputs = ["src/*", "CMakeLists.txt"]
outputs = ["build"]

[tasks.build]
cmd = "cmake --build build"
description = "Build the C++ project using CMake"
depends-on = ["_configure"]
inputs = ["src/*", "CMakeLists.txt"]
outputs = ["build/ramilass"]

[tasks.test]
cmd = "./build/run_tests"
description = "Run the test suite"
inputs = ["build/ramilass"]

[tasks.start]
inputs = ["build/ramilass"]
outputs = ["{{ output }}.gfa", "{{ output }}.fasta"]
args = [
    { "arg" = "file", "default" = "data/reads.fasta" },
    { "arg" = "output", "default" = "ramilass" },
]
cmd = "./build/ramilass $INIT_CWD/{{ file }} output --fuse --gfa --debug --max-contig-len 11000 --popping-passes 0"
description = "Run the app"

[tasks.ramilass]
cmd = "./build/ramilass"
description = "Run the app"
depends-on = ["build"]

[tasks.tofasta]
args = [
    { "arg" = "input", "default" = "./data/reads.fastq.fq" },
    { "arg" = "output", "default" = "./data/reads.fasta" }]
cmd = "sed ':a;N;$!ba;s/+\\n/+/g' {{ input }} | grep -v \"^+\" | sed \"s/@/>/g\" > {{ output }}"
inputs = ["{{ input }}"]
outputs = ["{{ output }}"]

[tasks._build_debug]
cmd = "rm -fr build && cmake -B build -DCMAKE_BUILD_TYPE=Debug && cmake --build build"
depends-on = ["_preparedir"]

[feature.memcheck.tasks.check_memory]
cmd = "valgrind --tool=memcheck --leak-check=full --show-leak-kinds=all --track-origins=yes build/ramilass data/reads.fasta --fuse"
depends-on = ["_build_debug"]
description = "Check memory leaks using Valgrind"

[dependencies]
cmake = ">=4.1.2,<5"
gtest = ">=1.17.0,<2"
cxx-compiler = ">=1.11.0,<2"
make = ">=4.4.1,<5"

[feature.quast.dependencies]
quast = { version = ">=5.3.0,<6", channel = "bioconda" }

[feature.bench.dependencies]
minia = { version = ">=3.2.6,<4", channel = "bioconda" }
spades = { version = ">=4.2.0,<5", channel = "bioconda" }

[feature.memcheck.dependencies]
valgrind = ">=3.25.1,<4"

[feature.plot.dependencies]
python = ">=3.8,<3.13"

[feature.plot.pypi-dependencies]
seaborn = "*"
pandas = "*"
matplotlib = "*"

[feature.plot.tasks.plotit]
cmd = "cd benchmark && python -s plot.py"
description = "Generate plots from benchmark results"
inputs = ["benchmark/plot.py", "benchmark/*.csv"]
outputs = ["benchmark/*.png"]

[feature.container.dependencies]
apptainer = "*"

[environments]
plot = { features = ["plot"], no-default-feature = true }
memcheck = { features = ["memcheck"], no-default-feature = true }
bench = { features = ["bench", "quast"], no-default-feature = true }
container = { features = ["container"], no-default-feature = true }
